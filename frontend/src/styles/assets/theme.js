import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#212124",
    },
    secondary: {
      main: "#161618",
    },
    background: {
      default: "#000000",
    },
    text: {
      white: "#ffffff",
    },
  },
});

export default theme;
